# Copy to .env.local for local dev. Gitignored by default.
#
# Base URL of the CrowdPatch API. Locally that's `wrangler dev` on 8787;
# in prod it's https://api.crowdpatch.dev (set in .env.production.local).
NEXT_PUBLIC_API_BASE=http://localhost:8787
